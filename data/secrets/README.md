# FlyWire API credentials (NOT committed)

Place your CAVE auth token here for connectome export scripts:

```
data/secrets/flywire_token.json
```

Or set environment variable:

```
FLYWIRE_TOKEN_FILE=/path/to/your/token.json
```

See `scripts/fetch_power_neurons.py` and FlyWire documentation for token setup.
